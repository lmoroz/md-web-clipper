{
	"name": "md-web-clipper",
	"version": "1.0.0",
	"description": "Save the readable part of a web page as a Markdown file (with images) or copy it to the clipboard.",
	"private": true,
	"scripts": {
		"build": "node build.mjs",
		"package": "node build.mjs && node package.mjs"
	},
	"dependencies": {
		"defuddle": "^0.19.1",
		"turndown": "^7.2.0"
	},
	"devDependencies": {
		"adm-zip": "^0.5.16",
		"esbuild": "^0.24.0"
	}
}
